# Convention: the AppProject name matches the app directory name and the
# Kargo Project name. Deliberately permissive for quickstart purposes —
# scope sourceRepos/destinations down before production use.
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: guestbook-helm
  namespace: argocd
spec:
  description: Guestbook deployed as a Helm chart, promoted by committing values bumps to main
  sourceRepos:
    - '*'
  destinations:
    - server: '*'
      name: '*'
      namespace: '*'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
