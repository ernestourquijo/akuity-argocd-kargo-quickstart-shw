# Chart defaults. Per-environment values live in ../env/<stage>/values.yaml
# and are layered on top by Argo CD (see argocd/appset.yaml).
replicaCount: 1

image:
  repository: ghcr.io/akuity/guestbook
  tag: latest

service:
  port: 80
